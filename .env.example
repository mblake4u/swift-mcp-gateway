# Base URL of the swift-token-server /proxy endpoint.
# In Docker Compose this is the internal service name.
# In dev (no Docker) use the exposed port.
PROXY_BASE_URL=http://swift-token-server:8080/proxy

# MCP server bind settings (defaults shown)
MCP_HOST=0.0.0.0
MCP_PORT=8080
